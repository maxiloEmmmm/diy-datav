// Code generated by entc, DO NOT EDIT.

package model

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/maxiloEmmmm/diy-datav/pkg/model/dataset"
	"github.com/maxiloEmmmm/diy-datav/pkg/model/view"
	"github.com/maxiloEmmmm/diy-datav/pkg/model/viewblock"
)

// ViewBlockCreate is the builder for creating a ViewBlock entity.
type ViewBlockCreate struct {
	config
	mutation *ViewBlockMutation
	hooks    []Hook
}

// SetType sets the "type" field.
func (vbc *ViewBlockCreate) SetType(s string) *ViewBlockCreate {
	vbc.mutation.SetType(s)
	return vbc
}

// SetConfig sets the "config" field.
func (vbc *ViewBlockCreate) SetConfig(s string) *ViewBlockCreate {
	vbc.mutation.SetConfig(s)
	return vbc
}

// SetViewID sets the "view" edge to the View entity by ID.
func (vbc *ViewBlockCreate) SetViewID(id int) *ViewBlockCreate {
	vbc.mutation.SetViewID(id)
	return vbc
}

// SetNillableViewID sets the "view" edge to the View entity by ID if the given value is not nil.
func (vbc *ViewBlockCreate) SetNillableViewID(id *int) *ViewBlockCreate {
	if id != nil {
		vbc = vbc.SetViewID(*id)
	}
	return vbc
}

// SetView sets the "view" edge to the View entity.
func (vbc *ViewBlockCreate) SetView(v *View) *ViewBlockCreate {
	return vbc.SetViewID(v.ID)
}

// AddDatasetIDs adds the "dataset" edge to the DataSet entity by IDs.
func (vbc *ViewBlockCreate) AddDatasetIDs(ids ...int) *ViewBlockCreate {
	vbc.mutation.AddDatasetIDs(ids...)
	return vbc
}

// AddDataset adds the "dataset" edges to the DataSet entity.
func (vbc *ViewBlockCreate) AddDataset(d ...*DataSet) *ViewBlockCreate {
	ids := make([]int, len(d))
	for i := range d {
		ids[i] = d[i].ID
	}
	return vbc.AddDatasetIDs(ids...)
}

// Mutation returns the ViewBlockMutation object of the builder.
func (vbc *ViewBlockCreate) Mutation() *ViewBlockMutation {
	return vbc.mutation
}

// Save creates the ViewBlock in the database.
func (vbc *ViewBlockCreate) Save(ctx context.Context) (*ViewBlock, error) {
	var (
		err  error
		node *ViewBlock
	)
	if len(vbc.hooks) == 0 {
		if err = vbc.check(); err != nil {
			return nil, err
		}
		node, err = vbc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ViewBlockMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = vbc.check(); err != nil {
				return nil, err
			}
			vbc.mutation = mutation
			node, err = vbc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(vbc.hooks) - 1; i >= 0; i-- {
			mut = vbc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, vbc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (vbc *ViewBlockCreate) SaveX(ctx context.Context) *ViewBlock {
	v, err := vbc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (vbc *ViewBlockCreate) check() error {
	if _, ok := vbc.mutation.GetType(); !ok {
		return &ValidationError{Name: "type", err: errors.New("model: missing required field \"type\"")}
	}
	if v, ok := vbc.mutation.GetType(); ok {
		if err := viewblock.TypeValidator(v); err != nil {
			return &ValidationError{Name: "type", err: fmt.Errorf("model: validator failed for field \"type\": %w", err)}
		}
	}
	if _, ok := vbc.mutation.Config(); !ok {
		return &ValidationError{Name: "config", err: errors.New("model: missing required field \"config\"")}
	}
	return nil
}

func (vbc *ViewBlockCreate) sqlSave(ctx context.Context) (*ViewBlock, error) {
	_node, _spec := vbc.createSpec()
	if err := sqlgraph.CreateNode(ctx, vbc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (vbc *ViewBlockCreate) createSpec() (*ViewBlock, *sqlgraph.CreateSpec) {
	var (
		_node = &ViewBlock{config: vbc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: viewblock.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: viewblock.FieldID,
			},
		}
	)
	if value, ok := vbc.mutation.GetType(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: viewblock.FieldType,
		})
		_node.Type = value
	}
	if value, ok := vbc.mutation.Config(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: viewblock.FieldConfig,
		})
		_node.Config = value
	}
	if nodes := vbc.mutation.ViewIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   viewblock.ViewTable,
			Columns: []string{viewblock.ViewColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: view.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.view_blocks = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := vbc.mutation.DatasetIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   viewblock.DatasetTable,
			Columns: []string{viewblock.DatasetColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: dataset.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ViewBlockCreateBulk is the builder for creating many ViewBlock entities in bulk.
type ViewBlockCreateBulk struct {
	config
	builders []*ViewBlockCreate
}

// Save creates the ViewBlock entities in the database.
func (vbcb *ViewBlockCreateBulk) Save(ctx context.Context) ([]*ViewBlock, error) {
	specs := make([]*sqlgraph.CreateSpec, len(vbcb.builders))
	nodes := make([]*ViewBlock, len(vbcb.builders))
	mutators := make([]Mutator, len(vbcb.builders))
	for i := range vbcb.builders {
		func(i int, root context.Context) {
			builder := vbcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ViewBlockMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, vbcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, vbcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, vbcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (vbcb *ViewBlockCreateBulk) SaveX(ctx context.Context) []*ViewBlock {
	v, err := vbcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
